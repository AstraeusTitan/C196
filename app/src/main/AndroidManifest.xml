<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.development.astraeus.c196">

    <application
        android:allowBackup="true"
        android:icon="@drawable/planning"
        android:label="@string/app_name"
        android:roundIcon="@drawable/planning"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TermListDisplay"
            android:label="@string/terms">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".TermDetailDisplay"
            android:label="@string/termDetail" />
        <activity
            android:name=".CourseListDisplay"
            android:label="@string/courses">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".CourseDetailDisplay"
            android:label="@string/courseDetail" />
        <activity
            android:name=".AssessmentListDisplay"
            android:label="@string/assessments">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".AssessmentDetailDisplay"
            android:label="@string/assessmentDetail"
            android:noHistory="true" />
        <activity
            android:name=".NoteDetailDisplay"
            android:label="@string/note_detail"
            android:noHistory="true"/>
    </application>

</manifest>